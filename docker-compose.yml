version: '3.8'

services:
  php-apache:
    build: ./php
    volumes:
     - ./htdocs:/var/www/html
    restart: always
    ports:
     - 8080:80
    depends_on:
     - mysql
  
  mysql:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
       MYSQL_ROOT_PASSWORD: '123qwecc'
volumes:
   db_data: {}


